# Equivalent to bool absval(int val, int& change_me)
# func absval val:int change_me:refint bool
func absval
    var bool ret
    if < val 0
        # assign change_me * -1 val
        assign ret True
        return True
    else
        # assign change_me val
        # return False
    endif
    return ret
endfunc

func main void
    var int val output
    assign val -5
    # funccall absval val outputfunccall print "The absolute value is: " output
    # funccall print "Did I negate the input value? " result
    funccall print val
    funccall print output
endfunc